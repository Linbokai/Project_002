---
description: AI Model Hub 项目技术栈、目录结构与通用编码规范
alwaysApply: true
---

# 项目概览

AI Model Hub — 全球AI模型信息聚合展示平台，基于 Vue 3 + TypeScript + MVVM 架构。

## 技术栈

- **框架**: Vue 3 (Composition API)
- **语言**: TypeScript (严格模式)
- **状态管理**: Pinia
- **路由**: Vue Router 4
- **构建工具**: Vite
- **样式**: TailwindCSS
- **HTTP**: Axios
- **包管理**: pnpm

## 目录结构

```
src/
├── assets/              # 静态资源
├── components/          # 通用可复用组件 (View 层)
│   ├── ui/              # 基础 UI 组件 (Button, Input, Modal...)
│   └── business/        # 业务组件 (由通用组件组合)
├── composables/         # 组合式函数 (ViewModel 层)
├── constants/           # 常量定义
├── models/              # 数据模型 (Model 层)
│   ├── types/           # TypeScript 接口/类型
│   └── enums/           # 枚举定义
├── router/              # 路由配置
├── services/            # API 与业务服务 (Model 层)
│   ├── api/             # API 请求封装
│   └── helpers/         # 业务辅助函数
├── stores/              # Pinia Store (ViewModel 层)
├── styles/              # 全局样式
├── utils/               # 通用工具函数
└── views/               # 页面组件 (View 层)
```

## 命名约定

- **文件命名**: kebab-case（如 `user-profile.vue`, `use-auth.ts`）
- **组件命名**: PascalCase（如 `UserProfile`, `BaseButton`）
- **变量/函数**: camelCase
- **常量**: UPPER_SNAKE_CASE
- **类型/接口**: PascalCase，接口不加 `I` 前缀
- **Pinia Store**: `use[Name]Store`

## Git 规范

- 分支命名: `feature/xxx`, `fix/xxx`, `refactor/xxx`
- Commit: `type(scope): description`，type 为 feat / fix / refactor / style / docs / test / chore
