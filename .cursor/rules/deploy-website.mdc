---
description: 当用户输入"更新网站"时，执行一键部署流程
alwaysApply: true
---

# 更新网站 — 一键部署流程

当用户说"更新网站"或类似的部署请求时，按以下步骤依次执行：

## 服务器连接信息

通过**堡垒机**连接，禁止直连 IP。

- 堡垒机地址: `pub.737.com`
- 堡垒机端口: `61022`
- 登录用户: `linbokai@btfx-prd-cn-sh-1`
- 私钥路径: `$env:USERPROFILE\.ssh\id_rsa`（Windows PowerShell）
- 远程工作目录: `~/ai-model-hub`
- Git 仓库: `https://github.com/Linbokai/Project_002.git`（Public 仓库）
- 外部访问地址: `http://106.15.191.84:82/`
- 内部服务端口: `8080`（运维通过端口映射 82 → 8080）

## 重要约束

- **禁止生成新 SSH 密钥** — 已有 `id_rsa` 密钥且运维已加入白名单，任何情况下都不要运行 `ssh-keygen`
- **禁止直连服务器 IP** — 必须通过堡垒机连接
- 如果 SSH 连接失败，报告错误并等待用户处理，不要自行尝试生成密钥或修改 SSH 配置

## 执行步骤

### Step 1 — 本地构建

```bash
pnpm build
```

如果构建失败，停止部署并报告错误。

### Step 2 — 提交并推送到 GitHub

将构建产物提交到 main 分支并推送：

```bash
git add dist/
git commit -m "build: update dist for deployment"
git push origin main
```

注意：`dist/` 已从 `.gitignore` 中移除，可以被 Git 追踪。如果当前不在 main 分支，先按项目 Git 规范合并到 main 后再推送。

### Step 3 — 服务器拉取最新代码

```powershell
echo 'cd ~/ai-model-hub && git pull origin main 2>&1 && echo PULL_OK; exit' | ssh -T -i "$env:USERPROFILE\.ssh\id_rsa" -p 61022 -o ConnectTimeout=30 -o ServerAliveInterval=30 "linbokai@btfx-prd-cn-sh-1@pub.737.com"
```

确认输出包含 `PULL_OK` 后继续。

### Step 4 — 重启服务

```powershell
echo 'cd ~/ai-model-hub && bash deploy/setup.sh; exit' | ssh -T -i "$env:USERPROFILE\.ssh\id_rsa" -p 61022 -o ConnectTimeout=30 -o ServerAliveInterval=30 "linbokai@btfx-prd-cn-sh-1@pub.737.com"
```

### Step 5 — 验证部署结果

检查 Step 4 输出中是否包含 `DEPLOY_SUCCESS`，向用户报告部署结果。

如果部署失败，执行以下命令查看日志：

```powershell
echo 'cat ~/ai-model-hub/server.log; exit' | ssh -T -i "$env:USERPROFILE\.ssh\id_rsa" -p 61022 -o ConnectTimeout=30 -o ServerAliveInterval=30 "linbokai@btfx-prd-cn-sh-1@pub.737.com"
```

## 服务器首次初始化（仅需执行一次）

如果服务器上还没有 Git 仓库，需要先执行初始化：

```powershell
echo 'cd ~ && rm -rf ai-model-hub && git clone --depth 1 https://github.com/Linbokai/Project_002.git ai-model-hub && echo CLONE_OK; exit' | ssh -T -i "$env:USERPROFILE\.ssh\id_rsa" -p 61022 -o ConnectTimeout=30 -o ServerAliveInterval=30 "linbokai@btfx-prd-cn-sh-1@pub.737.com"
```

初始化完成后，按正常 Step 3~5 执行即可。
