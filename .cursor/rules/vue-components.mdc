---
description: Vue 组件编码规范
globs: "**/*.vue"
alwaysApply: false
---

# Vue 组件规范

- 始终使用 `<script setup lang="ts">`
- Props 使用 `defineProps<T>()` 泛型语法
- Emits 使用 `defineEmits<T>()` 泛型语法
- 组件文件结构顺序：`<script setup>` → `<template>` → `<style scoped>`
- 样式必须使用 `scoped` 或 CSS Modules

## View 层要求

- `<script setup>` 中只做：引入 store/composable、解构、简单的 UI 状态
- 不在模板中编写业务逻辑，事件处理委托给 ViewModel 层
- 页面组件放 `src/views/`，可复用组件放 `src/components/`

## 示例

```vue
<script setup lang="ts">
import { onMounted } from 'vue'
import { useUserStore } from '@/stores/user-store'

const userStore = useUserStore()

onMounted(() => {
  userStore.fetchUsers()
})
</script>

<template>
  <div class="user-list">
    <div v-if="userStore.loading">加载中...</div>
    <ul v-else>
      <li v-for="user in userStore.users" :key="user.id">
        {{ user.name }}
      </li>
    </ul>
  </div>
</template>

<style scoped>
/* 样式 */
</style>
```
