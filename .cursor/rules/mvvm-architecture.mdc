---
description: MVVM 分层架构规则 — 定义 Model / ViewModel / View 职责与数据流
alwaysApply: true
---

# MVVM 架构规范

## 数据流

```
View (用户操作) → ViewModel (处理逻辑) → Model (数据/API)
Model (数据响应) → ViewModel (状态更新) → View (响应式渲染)
```

## Model 层（数据与业务逻辑）

- 位于 `src/models/` 和 `src/services/`
- `models/` — TypeScript 类型定义、接口、数据模型
- `services/` — API 调用、数据持久化、业务逻辑服务
- **禁止**包含任何 UI 相关逻辑或 Vue 组件引用
- 所有 API 请求必须通过 service 层封装，组件不可直接调用 axios

## ViewModel 层（状态与交互逻辑）

- 位于 `src/stores/` (Pinia) 和 `src/composables/`
- `stores/` — Pinia Store，管理全局/模块级状态
- `composables/` — 可复用的组合式函数 (useXxx)，封装组件级状态逻辑
- 负责：状态管理、数据转换、表单验证、用户交互逻辑
- **禁止**在 Store/Composable 中直接操作 DOM
- Store 命名: `use[Module]Store`，Composable 命名: `use[Feature]`

## View 层（UI 展示）

- 位于 `src/views/` 和 `src/components/`
- `views/` — 页面级组件，对应路由
- `components/` — 可复用 UI 组件
- 只负责：模板渲染、样式、事件绑定（委托给 ViewModel）
- **禁止**在 `<template>` 或 `<style>` 中包含业务逻辑
- `<script setup>` 中只做：引入 store/composable、解构、简单的 UI 状态

## 状态管理规范

- 简单组件状态用 `ref`/`reactive`
- 跨组件共享状态用 Pinia Store
- Store 按功能模块拆分，避免单一巨大 Store
- 异步操作统一通过 `actions` 处理
- 禁止在组件中直接修改 Store state，必须通过 actions

## API 调用规范

- 所有 API 调用封装在 `services/api/` 下
- 每个模块一个 API 文件（如 `user-api.ts`, `order-api.ts`）
- API 函数必须有明确的请求/响应类型定义
- 统一错误处理通过 Axios 拦截器实现
