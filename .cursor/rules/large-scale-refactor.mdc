---
description: 大规模功能调整/重构的标准化工作流程
alwaysApply: true
---

# 大规模调整工作流

当用户要求进行**跨多个模块的大规模调整**（涉及 3+ 个功能模块或 10+ 个文件）时，必须遵循以下流程，禁止直接动手改代码。

## 判定标准

以下任一条件满足即视为"大规模调整"：

- 涉及 3 个以上功能模块
- 预计修改 10 个以上文件
- 需要新增 3 个以上文件
- 用户明确要求"全面升级/重构/大改"

## 第一步：探索与分析

1. 用 explore subagent 扫描项目结构，产出涉及文件清单与行数
2. 识别**共享文件**（被多模块依赖的类型/常量/Store），标记为高风险
3. 梳理模块间依赖关系，画出依赖图

## 第二步：创建总控计划文档

在 `.cursor/plans/` 下创建 Markdown 文档，必须包含：

```
- 项目概况（技术栈、代码规模、关键大文件）
- 共享文件清单（文件路径 + 被依赖情况）
- 模块执行顺序与依赖关系图
- 每个模块的详细任务卡：
  - 状态标记（🔴待开始 / 🟡进行中 / 🟢已完成）
  - 优先级（P0/P1/P2）
  - 涉及文件列表（现有 + 新建）
  - 具体任务清单（可执行的条目）
  - 与其他模块的依赖说明
- Cloud Agent 使用指南（prompt 模板）
- 变更日志表格
```

## 第三步：确定执行策略

根据模块间依赖关系，将任务分为可并行的 Phase：

- **同一 Phase 内的模块**：无共享文件冲突，可并行执行
- **不同 Phase 之间**：串行执行，前一 Phase 完成后再启动下一 Phase
- 每个模块创建独立 Git 分支：`feature/m{编号}-{模块名}`

## 第四步：逐模块执行（四阶段循环）

每个模块遵循：

1. **Plan** — 用 Plan Mode 或在 Cloud Agent 中生成详细方案，等用户批准
2. **Execute** — 按方案执行代码修改
3. **Verify** — `pnpm build` 验证无错误，对比 main 分支 review
4. **Commit** — 提交代码，更新总控计划文档状态

## 第五步：完成后更新

每个模块完成后，在总控计划文档中：

- 状态改为 🟢
- 记录实际改动的文件列表
- 记录新增的类型/接口/组件名
- 记录遇到的问题和解决方案

## 执行约束

- **禁止跳过计划阶段直接写代码**
- **禁止同时修改共享文件** — 如果两个并行模块都需要改同一共享文件，必须串行
- **失败时回滚重来** — 不在错误基础上修补，revert 后重新规划
- 每个模块的改动应可独立运行和测试
